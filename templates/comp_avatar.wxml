<view class="avatar-container">
  <image class="avatar" src="{{ $avatar.url }}" mode="aspectFill" catchtap="$avatar_userMenu"/>
  <text class="name">{{ $avatar.name }}</text>
  <text class="identity">{{ $avatar.identity }}</text>
</view>